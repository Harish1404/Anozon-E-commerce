context: we're facing lot of UI bugs currently

for example: 

when user add a cart and the cart count is updated and after that if we logout then cart count is still in the cart icon  and also after i can still add the cart after logout the cart item is still in the cart after i clicks the refreshes browser it back to normal but again if i loggedIn and in cart i can see the items which was added after logout this is crictcal bug

suppose if a backend server is down this page retries for fetch updates one the backend was up all works well but the navbar profile drop menu is still in signin after i refresh the page gets updates and show sthe profile and all 



1. 



